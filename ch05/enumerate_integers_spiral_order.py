#!/usr/bin/env python3


def enumerate_integers_spiral_order(n: int) -> list[tuple]:
    spiral_order: list = []

    for i in range(n):
        current: tuple = ()
        spiral_order += current

    return spiral_order
